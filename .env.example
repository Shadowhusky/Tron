# Tron — Environment Configuration
# Copy this file to .env and adjust as needed.

# ─── Server Deployment ───────────────────────────────────────────────
# TRON_MODE: Deployment mode for the web server.
#   "local"   — Full local PTY + SSH (default, self-hosted)
#   "gateway" — Cloud/hosted deployment (node-pty optional, SSH-only by default)
TRON_MODE=local

# TRON_SSH_ONLY: Restrict to SSH connections only.
#   When true, users must connect via SSH before opening a terminal tab.
#   Local terminal, file operations, and server shell access are blocked.
#   Gateway mode defaults to true; set to false to allow local terminal in gateway.
TRON_SSH_ONLY=false

# PORT: Vite dev server port (used by dev:web)
# PORT=5173

# ─── API Keys (for scripts/update-models.ts) ────────────────────────
# Only providers with keys will be fetched; others use hardcoded fallbacks.
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
GEMINI_API_KEY=AIza...
DEEPSEEK_API_KEY=sk-...
KIMI_API_KEY=
QWEN_API_KEY=
GLM_API_KEY=
MINIMAX_API_KEY=
