# E2E Test Configuration
# Copy this file to .env.test and adjust values for your setup.
#
#   cp .env.test.example .env.test
#
# Most tests run without any AI provider. Only the agent tests (08-agent)
# require a live provider and are skipped when TEST_PROVIDER is unset.

# AI provider to use for agent tests (ollama | lmstudio | openai | anthropic | gemini | deepseek | etc.)
# TEST_PROVIDER=ollama

# Model name as shown in the provider's model list
# TEST_MODEL=llama3.2

# Base URL for local providers (Ollama, LM Studio, OpenAI-compatible)
# TEST_BASE_URL=http://localhost:11434

# API key for cloud providers (OpenAI, Anthropic, Gemini, DeepSeek, etc.)
# TEST_API_KEY=sk-...
